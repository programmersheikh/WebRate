@model IEnumerable<WebRate.Models.Comment>

@{
    ViewBag.Title = "Index";
}

<h2>Comments</h2>

<p>
    @Html.ActionLink("Post Comment", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CommentDesc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TopicID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WebID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CommentDesc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TopicID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WebID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CommntID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CommntID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CommntID })
        </td>
    </tr>
}

</table>
